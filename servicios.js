function mostrarMenu() {
    if (menuImagenes.style.display === "none") {
        // Si el menú está oculto, lo mostramos
        menuImagenes.style.display = "block";
    } else {
        // Si el menú está visible, lo ocultamos
        menuImagenes.style.display = "none";
    }
}